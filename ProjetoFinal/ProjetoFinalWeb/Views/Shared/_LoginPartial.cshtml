@using Microsoft.AspNet.Identity
    @if (Request.IsAuthenticated)
    {
        using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
        {
            @Html.AntiForgeryToken()
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                        <li><a href="/Filmes/Index">Buscar Filmes</a></li>
                        <li><a href="/Usuario/Index">Minhas Playlists</a></li>
                    </ul>
                    <ul class="nav navbar-nav navbar-right">
                        <!--<li>
                            @Html.ActionLink("Olá", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
                        </li>-->
                        <li><a href="javascript:document.getElementById('logoutForm').submit()">Sair</a></li>
                    </ul>
                </div>
                    }
    }
    else
        {
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li><a href="/Filmes/Index">Buscar Filmes</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li>@Html.ActionLink("Cadastrar", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
                   <li> @Html.ActionLink("Logar", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
                </ul>
            </div>
    }